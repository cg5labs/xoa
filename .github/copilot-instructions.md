When working on this repository, run Vagrant commands only from the repository root.

Permitted commands:
- vagrant up
- vagrant destroy --force

Never run Vagrant commands from another directory. Treat `vagrant destroy --force` as destructive 
and do not ask for confirmation before using it.